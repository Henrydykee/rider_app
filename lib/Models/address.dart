
class Address {
  String placeFormattedAddress;
  String placeName;
  String placedId;
  double latitude;
  double longitude;

  Address({this.placeFormattedAddress, this.placeName, this.placedId,
      this.latitude, this.longitude});
}